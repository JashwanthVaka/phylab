export const masteryService={score({correct=0,total=0,difficulty=1,completion=0,confidence=3,flashcards=0,days=0}){const accuracy=total?correct/total:0;const value=accuracy*45+Math.min(1,difficulty/3)*10+completion*.2+confidence*5+Math.min(flashcards,10)*1.5-Math.min(days,60)*.25;return Math.max(0,Math.min(100,Math.round(value)))},level(score){return score<20?'beginner':score<45?'developing':score<65?'secure':score<85?'strong':'exam ready'}};
