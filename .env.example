# KINETIQ server environment. Never commit a real key — .env is git-ignored.
# Add ONE OR MORE AI providers. KINETIQ uses the first configured provider,
# and learners can switch between any that are configured.

# Vercel AI Gateway. On Vercel, VERCEL_OIDC_TOKEN is supplied automatically,
# so no browser-visible key is needed. Set this only for local/non-Vercel use.
AI_GATEWAY_API_KEY=
AI_GATEWAY_MODEL=openai/gpt-5.6-sol

# Groq — fast and free-tier friendly. https://console.groq.com/keys
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# OpenAI — https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-sol

# Anthropic — https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# Google Gemini — https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Optional: force one provider instead of the first configured one.
AI_PROVIDER=

PORT=3000
HOST=127.0.0.1

# Browser-safe Supabase project values used by the server and client.
SUPABASE_URL=
SUPABASE_ANON_KEY=
