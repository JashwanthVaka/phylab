import {guestConversationService as s} from '../js/services/guestConversationService.js'; const m=new Map;const storage={getItem:k=>m.get(k)||null,setItem:(k,v)=>m.set(k,v)};const c=s.create('Test',storage);s.message(c.id,{id:'m1',content:'hello'},storage);s.message(c.id,{id:'m1',content:'hello'},storage);if(s.list(storage)[0].messages.length!==1)throw Error('duplicate');s.update(c.id,{title:'Renamed'},storage);if(s.list(storage)[0].title!=='Renamed')throw Error('rename');s.remove(c.id,storage);if(s.list(storage).length)throw Error('delete');console.log('guest conversation tests passed');
