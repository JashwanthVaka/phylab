import {physics} from '../js/physicsEngine.js'; const near=(a,b)=>{if(Math.abs(a-b)>1e-6)throw Error(`${a} != ${b}`)};near(physics.projectile({v:10,angle:45,gravity:10}).range,10);near(physics.newton({force:20,mass:4}).acceleration,5);near(physics.energy({mass:2,height:3,velocity:4,gravity:10}).total,76);near(physics.photon({f:1}).energy,6.62607015e-34);near(physics.decay({N0:100,halfLife:2,time:2}).remaining,50);console.log('physics-engine known cases passed');
