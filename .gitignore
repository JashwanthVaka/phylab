# Every .env variant is a secret, except the committed template.
.env*
!.env.example

node_modules
npm-debug.log*
.DS_Store
__pycache__/
*.pyc

# Personal study sources. Never commit copyrighted books or their extracted text.
private/
dist/

# Static build output. Vercel runs the real server; these are only for a static fallback.
docs/
